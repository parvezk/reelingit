# @reelingit/web

Next.js web app for ReelingIt. Never reads `DATABASE_URL`; all data goes through the BFF API.

## Status

Stub only — filled in by [PK-131](https://linear.app/pk-hq/issue/PK-131) (Next.js App Router + Tailwind + shadcn/ui) and the rest of M3 / M4.

## Env (planned)

- `NEXT_PUBLIC_API_URL` — base URL of `@reelingit/api`
